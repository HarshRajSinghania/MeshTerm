"""Visualization layer: Plotly / pyvis renderers producing interactive HTML (and PNG).

Planned renderers (see the project plan):

* ``tx_plot`` — SNR-vs-TX-power curve with confidence band and chosen optimum.
* ``path_graph`` — interactive pyvis mesh graph, edges colored by SNR.
* ``timeseries`` — link SNR / success-rate dashboards over time.

This package is intentionally empty in the skeleton; renderers are added alongside the
TX-power and path-optimization tools.
"""
